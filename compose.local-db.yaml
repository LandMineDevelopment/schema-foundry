# Linux-only override for PostgreSQL bound to host loopback.
services:
  schemii:
    network_mode: host
    ports: !reset []
    environment:
      SCHEMII_HOST: 127.0.0.1
      SCHEMII_BEHIND_LOOPBACK_PROXY: 0
