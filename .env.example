# Optional included PostgreSQL settings. Copy this file to .env and edit it
# before the first default launch, then continue to use start.sh or start.ps1.
# Change the password before using the stack beyond local evaluation.
SCHEMII_POSTGRES_DB=schemii
SCHEMII_POSTGRES_USER=schemii
SCHEMII_POSTGRES_PASSWORD=schemii-local
